export class Permissions {
  // TODO: implement permission checks
  requestLocation(): boolean { return true; }
  requestMedia(): boolean { return true; }
  requestAudio(): boolean { return true; }
}
